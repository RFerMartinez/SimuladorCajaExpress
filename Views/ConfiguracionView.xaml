<UserControl x:Class="CajaExpressSim.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource ColorFondoPrincipal}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="CAJA EXPRESS ANALYTICS | " FontSize="20" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Configuración de Parámetros" FontSize="20" Foreground="{StaticResource ColorTextoSecundario}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">

                <Border Style="{StaticResource EstiloTarjeta}">
                    <StackPanel>
                        <TextBlock Text="Recursos Generales" Style="{StaticResource TituloTarjeta}" Foreground="White" FontSize="16"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="Tiempo de Simulación (Semanas):" Foreground="#A0AEC0" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding Semanas}" FontSize="16" FontWeight="Bold" Height="35"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Días Laborables por Semana:" Foreground="#A0AEC0" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding DiasPorSemana}" FontSize="16" Height="35"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Cantidad de Cajas (Servidores):" Foreground="#A0AEC0" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding CantidadCajas}" FontSize="14" Margin="0,0,0,15" Height="35"/>

                        <TextBlock Text="Tiempo de Cobro Adicional (Segundos):" Foreground="#A0AEC0" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TiempoCobro}" FontSize="14" Height="35"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource EstiloTarjeta}" Margin="5,15,5,5">
                    <StackPanel>
                        <TextBlock Text="Tasas de Llegada (Clientes por Hora)" Style="{StaticResource TituloTarjeta}" Foreground="White" FontSize="16"/>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Franja 1 (08:00 - 12:00):" Foreground="#A0AEC0" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding TasaLlegada1}" Height="30"/>
                        </Grid>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Franja 2 (12:00 - 15:00):" Foreground="#A0AEC0" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding TasaLlegada2}" Height="30"/>
                        </Grid>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Franja 3 (15:00 - 22:00):" Foreground="#A0AEC0" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding TasaLlegada3}" Height="30"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Column="2" Style="{StaticResource EstiloTarjeta}">
                <StackPanel>
                    <TextBlock Text="Tiempos de Servicio (Normal)" Style="{StaticResource TituloTarjeta}" Foreground="White" FontSize="16"/>

                    <TextBlock Text="1. CLIENTE ESTÁNDAR (60%)" FontWeight="Bold" Foreground="White" Margin="0,10,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Media μ:" Foreground="#A0AEC0"/>
                            <TextBox Text="{Binding MediaEstandar}" Height="30"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Desviación σ:" Foreground="#A0AEC0"/>
                            <TextBox Text="{Binding DesvioEstandar}" Height="30"/>
                        </StackPanel>
                    </Grid>

                    <Separator Background="#2C3545" Margin="0,15"/>

                    <TextBlock Text="2. CLIENTE EXPRESS (30%)" FontWeight="Bold" Foreground="White" Margin="0,0,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Media μ:" Foreground="#A0AEC0"/>
                            <TextBox Text="{Binding MediaExpress}" Height="30"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Desviación σ:" Foreground="#A0AEC0"/>
                            <TextBox Text="{Binding DesvioExpress}" Height="30"/>
                        </StackPanel>
                    </Grid>

                    <Separator Background="#2C3545" Margin="0,15"/>

                    <TextBlock Text="3. CARRO COMPLETO (10%)" FontWeight="Bold" Foreground="White" Margin="0,0,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Media μ:" Foreground="#A0AEC0"/>
                            <TextBox Text="{Binding MediaCarro}" Height="30"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Desviación σ:" Foreground="#A0AEC0"/>
                            <TextBox Text="{Binding DesvioCarro}" Height="30"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Button Grid.Row="2" Content="GUARDAR CONFIGURACIÓN" 
                Command="{Binding GuardarCommand}"
                Style="{StaticResource BotonVerde}"
                Background="{StaticResource ColorAccionVerde}"
                Height="50" Margin="0,20,0,0" FontSize="16"/>
    </Grid>
</UserControl>