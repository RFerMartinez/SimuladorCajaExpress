<UserControl x:Class="CajaExpressSim.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1100"
             Background="{StaticResource ColorFondoPrincipal}">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="Lbl">
            <Setter Property="Foreground" Value="#A0AEC0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Background" Value="#2D3748"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4A5568"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="HeaderTitle">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="CAJA EXPRESS ANALYTICS | " FontSize="24" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Configuración de Parámetros" FontSize="24" Foreground="{StaticResource ColorTextoSecundario}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                <StackPanel>
                    <Border Style="{StaticResource EstiloTarjeta}" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Recursos Generales" Style="{StaticResource HeaderTitle}"/>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Tiempo de Simulación (Semanas):" Style="{StaticResource Lbl}"/>
                                    <TextBox Text="{Binding Semanas}" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Días Laborables por Semana:" Style="{StaticResource Lbl}"/>
                                    <TextBox Text="{Binding DiasPorSemana}"/>
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Cantidad de Cajas (Servidores):" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding CantidadCajas}"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Capacidad Máxima de Cola (Personas por Caja):" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding CapacidadCola}"/>
                            </StackPanel>

                            <StackPanel>
                                <TextBlock Text="Tiempo de Cobro Adicional (Segundos):" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding TiempoCobro}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource EstiloTarjeta}" Margin="0,20,0,5" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Tasas de Llegada (Clientes por Hora)" Style="{StaticResource HeaderTitle}"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Franja 1 (08:00 - 12:00):" Style="{StaticResource Lbl}"/>
                                <TextBox Grid.Column="1" Text="{Binding TasaLlegada1}"/>
                            </Grid>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Franja 2 (12:00 - 15:00):" Style="{StaticResource Lbl}"/>
                                <TextBox Grid.Column="1" Text="{Binding TasaLlegada2}"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Franja 3 (15:00 - 22:00):" Style="{StaticResource Lbl}"/>
                                <TextBox Grid.Column="1" Text="{Binding TasaLlegada3}"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                <Border Style="{StaticResource EstiloTarjeta}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Tiempos de Servicio (Normal)" Style="{StaticResource HeaderTitle}"/>

                        <TextBlock Text="1. CLIENTE ESTÁNDAR (60%)" FontWeight="Bold" Foreground="White" FontSize="15" Margin="0,5,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Media μ:" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding MediaEstandar}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Desviación σ:" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding DesvioEstandar}"/>
                            </StackPanel>
                        </Grid>

                        <Separator Background="#2C3545" Margin="0,20"/>

                        <TextBlock Text="2. CLIENTE EXPRESS (30%)" FontWeight="Bold" Foreground="White" FontSize="15" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Media μ:" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding MediaExpress}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Desviación σ:" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding DesvioExpress}"/>
                            </StackPanel>
                        </Grid>

                        <Separator Background="#2C3545" Margin="0,20"/>

                        <TextBlock Text="3. CARRO COMPLETO (10%)" FontWeight="Bold" Foreground="White" FontSize="15" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Media μ:" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding MediaCarro}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Desviación σ:" Style="{StaticResource Lbl}"/>
                                <TextBox Text="{Binding DesvioCarro}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>