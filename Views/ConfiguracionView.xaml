<UserControl x:Class="CajaExpressSim.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Parámetros de Simulación" FontSize="24" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">

                <GroupBox Header="Duración de la Simulación" Padding="10" Margin="0,0,0,15" BorderBrush="#007ACC">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Semanas a Simular:"/>
                            <TextBox Text="{Binding Semanas}" Margin="0,5,0,0" Padding="3" FontWeight="Bold"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Días Laborables/Semana:"/>
                            <TextBox Text="{Binding DiasPorSemana}" Margin="0,5,0,0" Padding="3"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Recursos Generales" Padding="10" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Cantidad de Cajas (Servidores):"/>
                        <TextBox Text="{Binding CantidadCajas}" Margin="0,5,0,10" Padding="3"/>

                        <TextBlock Text="Tiempo de Cobro Adicional (Segundos):"/>
                        <TextBox Text="{Binding TiempoCobro}" Margin="0,5,0,10" Padding="3"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Tasas de Llegada (Clientes por Hora)" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Franja 1 (08:00 - 12:00) [Clientes/Hora]:"/>
                        <TextBox Text="{Binding TasaLlegada1}" Margin="0,5,0,10" Padding="3"/>

                        <TextBlock Text="Franja 2 (12:00 - 15:00) [Clientes/Hora]:"/>
                        <TextBox Text="{Binding TasaLlegada2}" Margin="0,5,0,10" Padding="3"/>

                        <TextBlock Text="Franja 3 (15:00 - 22:00) [Clientes/Hora]:"/>
                        <TextBox Text="{Binding TasaLlegada3}" Margin="0,5,0,10" Padding="3"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>

            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <GroupBox Header="Tiempos de Servicio (Distribución Normal)" Padding="10">
                        <StackPanel>
                            <TextBlock Text="1. CLIENTE ESTÁNDAR" FontWeight="Bold" Foreground="#007ACC" Margin="0,5,0,5"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Media (μ):"/>
                                    <TextBox Text="{Binding MediaEstandar}" Margin="0,5,0,0" Padding="3"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Desviación (σ):"/>
                                    <TextBox Text="{Binding DesvioEstandar}" Margin="0,5,0,0" Padding="3"/>
                                </StackPanel>
                            </Grid>

                            <Separator Margin="0,0,0,10"/>
                            <TextBlock Text="2. CLIENTE EXPRESS (Rápido)" FontWeight="Bold" Foreground="#E65100" Margin="0,0,0,5"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Media (μ):"/>
                                    <TextBox Text="{Binding MediaExpress}" Margin="0,5,0,0" Padding="3"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Desviación (σ):"/>
                                    <TextBox Text="{Binding DesvioExpress}" Margin="0,5,0,0" Padding="3"/>
                                </StackPanel>
                            </Grid>

                            <Separator Margin="0,0,0,10"/>
                            <TextBlock Text="3. CARRO COMPLETO (Lento)" FontWeight="Bold" Foreground="#2E7D32" Margin="0,0,0,5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Media (μ):"/>
                                    <TextBox Text="{Binding MediaCarro}" Margin="0,5,0,0" Padding="3"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Desviación (σ):"/>
                                    <TextBox Text="{Binding DesvioCarro}" Margin="0,5,0,0" Padding="3"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Button Grid.Row="2" Content="Guardar Configuración" 
                Command="{Binding GuardarCommand}"
                Height="40" Width="200" HorizontalAlignment="Right" Margin="0,20,0,0"
                Background="#007ACC" Foreground="White" FontWeight="Bold"/>
    </Grid>
</UserControl>