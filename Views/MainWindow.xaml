<Window x:Class="CajaExpressSim.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CajaExpressSim.ViewModels"
        xmlns:views="clr-namespace:CajaExpressSim.Views"
        mc:Ignorable="d"
        Title="Simulador Caja Express 2025" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#FAFAFA">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ConfiguracionViewModel}">
            <views:ConfiguracionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ResultadosViewModel}">
            <views:ResultadosView/>
        </DataTemplate>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#333333" Padding="20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🛒" FontSize="24" Margin="0,0,10,0"/>
                <TextBlock Text="CAJA EXPRESS - TALLER DE INTEGRACIÓN" 
                           Foreground="White" FontWeight="Bold" FontSize="18" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding VistaActual}" Margin="20"/>

        <Border Grid.Row="2" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="← Volver a Configuración" 
                        Command="{Binding VolverConfiguracionCommand}"
                        Padding="15,10" Background="Transparent" BorderBrush="#CCC" BorderThickness="1"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Estado del Sistema: " FontWeight="Bold" Foreground="#555"/>
                    <TextBlock Text="{Binding EstadoSimulacion}" Foreground="#007ACC" Margin="5,0,0,0"/>

                    <TextBlock Text=" ⏳ Procesando..." Foreground="#FF8C00" FontWeight="Bold"
                               Visibility="{Binding EstaSimulando, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </StackPanel>

                <Button Grid.Column="2" Content="▶ INICIAR SIMULACIÓN" 
                        Command="{Binding IniciarSimulacionCommand}"
                        Padding="20,10" Background="#2E7D32" Foreground="White" FontWeight="Bold" FontSize="14"
                        BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>